services:
  - type: web
    name: cricket-ultimate-frontend
    runtime: static
    region: singapore
    plan: free
    branch: gh-pages
    buildCommand: "true"
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
